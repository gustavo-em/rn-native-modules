import styled from "styled-components/native";
import { colors } from "../../../colors";


export const WrapperScreensStyle = styled.SafeAreaView`
    flex: 1;
    background-color: ${colors.black10};
`
